const initialCartState = {
    cart: {},
};

function cartReducer(state = initialCartState, action) {
    switch (action.type) {
        // coming soon
        default:
            return state;
    }
}

export default cartReducer;
